schema_version: 1
target: agentcore
deployment_profile: development
region: us-east-1

network:
  mode: public

control_plane:
  enabled: true
  access: public-authenticated
  hostname:
    mode: generated

identity:
  mode: managed-cognito

runtime:
  state: active
  providers:
    - bedrock
